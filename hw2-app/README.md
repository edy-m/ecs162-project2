In my gradescope submission I had to remove the modules folder as well as the .git folder because there were way too many files for gradescope to handle, so here I'm posting the link to my project repository so you can view the .git
LINK: https://github.com/edy-m/ecs162-project2.git

I implemented my project using the html/css/js approach (no svelte) so my project folder is ordered as instructed, also I worked on this project alone, by the time I realized there was a partner requirement I had already made significant progress and wasn't able to find a partner in time. Also, the link 
https://localhost:8000
to visit our web page doesn't seem to work on my machine, it only works if we remove the s from https as such:
http://localhost:8000
so please test it this way